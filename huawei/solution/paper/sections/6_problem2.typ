= 6 问题二：异构多点运输与共享电池调度

== 6.1 路线、时限与资源模型

一个候选架次由货箱集合、访问顺序、机型和逐段载荷轨迹组成。出航携带全部货箱，每完成一次交接即减少后续航段载荷；所有架次最后返回 O01。程序对至多四个服务区的候选路线枚举访问顺序，逐条筛选质量、体积、能耗与返航余量。实体无人机自开始至返航被占用；电池自开始至返航后再充满被占用，故二者不能作为一个资源合并安排。

医疗物资以其期望送达时刻为硬截止，首批货箱以首批截止时刻为硬约束，其余箱的软延期成本为 $w_b max(0,T_b-tau_b)$。方案优先消除硬截止违约，再依次比较总超时、加权延期、全部运输机最晚返航时刻、总能耗和架次数。这种词典序避免以少量能源收益交换医疗物资的时限。对每条候选路线，资源解码器选择最早可用且同型号的实体机与电池，记录任务和充电区间。

== 6.2 搜索与计算结果

求解以问题一的单点组批生成可行初始路线，对能合并的路线逐对比较，随后采用带随机、最差和相关路线移除算子的自适应大邻域搜索；贪心与二阶遗憾插入负责修复，历史表现更新算子权重，模拟退火允许有限的非改进移动[2]。固定随机种子为 2026，搜索 120 轮。候选站数与迭代次数是计算预算，所得结果为可复核的启发式解。

初始 31 架次经搜索降为 21 架次，80 箱全部唯一交付，硬时限违约数为 0。加权延期为 186162.124 优先系数·秒，运输机最晚返航时刻 11420.942 s，运输能耗 73.387519 kWh。质量、体积、返航 SOC、实体机任务重叠和电池充电重叠的逐项检查均通过。

#figure(
  placement: top,
  image("../../runs/reproduced/figures/q2_routes.pdf", width: 82%),
  caption: [问题二多点运输路线与服务区访问。],
)

路线图显示同一架次可跨服务区投送，这与问题一按服务区独立组批的任务结构不同。跨区合并减少了部分单点重复返航，但配送及时性仍由逐箱时刻而非路线长度单独决定。

#figure(
  placement: top,
  image("../../runs/reproduced/figures/q2_uav_gantt.pdf", width: 82%),
  caption: [问题二实体运输无人机占用时序。],
)

甘特图给出不同实体机的执行区间。与工作簿的共享电池充电区间合并核查时，同一资源没有重叠；因此 21 架次不仅在路径和能耗层可行，也能落实到有限的实体资源。
