= 5 问题一：安全载荷与单点组批

== 5.1 最大安全载荷

对每个机型—服务区组合，先计算 O01→服务区的满载出航与空载返航能耗。出航能耗随载荷单调不减，因而可在 $[0,Q_g]$ 内二分反求最大安全载荷；当额定载重已满足返航余量时直接取额定值。15 个服务区乘三型机共形成 45 个值。20% 返航余量下，A 型全为 25.000 kg，B 型为 28.632—30.000 kg，C 型为 58.436—80.000 kg。高额定载荷并不保证山区航段可用，个别 B/C 型组合受爬升能耗与返航余量同时约束。

#figure(
  placement: top,
  image("../../runs/reproduced/figures/q1_safe_payload_heatmap.pdf", width: 82%),
  caption: [三型机在 15 个服务区的最大安全载荷。],
)

热力图展示 45 个结果。若只按额定载荷分派货箱，会将部分远距或高爬升服务区错误判为可运，因此后续组批逐组合复核能量，而不只比较质量和体积。

== 5.2 不可拆货箱的精确组批

对每个服务区的货箱集合，枚举非空子集。对每个子集筛选满足质量、体积和返航 SOC 的机型，并保留该子集能耗最低、再按时间择优的机型。位掩码动态规划要求每箱恰好属于一个子集，按“架次数—总能耗—累计作业时间”词典序比较所有可行划分。服务区相互独立，故各区最优解相加构成问题一规定的单点运输方案。该精确性只针对本问的固定单点直接往返与给定三型机选择。

结果为 18 架次、总能耗 59.231986 kWh、累计作业时间 32777.291 s，80 个货箱无遗漏或重复，最低返航 SOC 为 22.7893%。独立的首次适配递减装箱基线同为 18 架次但能耗 59.504051 kWh，精确组批在架次数相同下少耗约 0.272065 kWh。完整逐架次货箱、质量、体积与 SOC 见提交工作簿。

== 5.3 安全余量变化

#figure(
  table(
    columns: (1fr, 1fr, 1.3fr, 1.4fr),
    stroke: 0.35pt,
    inset: 5pt,
    [返航余量], [最少架次], [总能耗/kWh], [最低返航 SOC],
    [15%], [18], [59.231986], [22.789%],
    [20%], [18], [59.231986], [22.789%],
    [25%], [19], [61.181401], [26.782%],
    [30%], [20], [67.349440], [30.751%],
  ),
  caption: [返航安全余量敏感性。],
)

15% 至 20% 时最优组批保持 18 架次，说明当前架次的最低 SOC 仍高于两个阈值；余量升至 25% 和 30% 后分别增加至 19 和 20 架次，显示货箱不可拆造成的离散跳变。总能耗并非仅由余量公式连续缩放，而取决于重新选择的组批和机型。
