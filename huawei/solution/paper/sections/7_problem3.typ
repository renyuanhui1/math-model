= 7 问题三：连续通信下的运输—中继联合方案

== 7.1 双向链路与候选悬停点

固定网关 G01 的天线高度、运输机实时海拔和中继机悬停海拔由附件给定。两端点之间以 DEM 检测视线遮挡，传播损耗由自由空间损耗与题给遮挡附加损耗相加；自由空间部分按 ITU-R P.525 的频率和三维距离关系计算[3]。对每条通信链路分别计算两个方向允许的最大损耗，采用更严格的一侧判定可用。直连失败时，中继机的接入链路与其至网关的回传链路必须在同一时刻同时可用；不允许中继之间再多跳。

先将问题二方案转成爬升、巡航、下降与交接的 1 秒轨迹网格，定位直连盲点，再在盲区邻域和地形高点产生位于 DEM 范围内的悬停候选。每个候选需通过回传链路与离地高度筛选。中继架次计入 O01 准备、往返飞行、30 秒建链、悬停通信、返航余量和能源组件充电。若中继资源冲突或某盲段无法覆盖，允许调整相关运输机的路线组合与出发时刻，再复核货箱硬时限。

== 7.2 联合调度结果与权衡

最终方案由 21 个运输架次、10 个中继架次和 4 个悬停点组成。运输与中继总能耗分别为 78.790481 和 8.053871 kWh，合计 86.844352 kWh；联合最晚返航时刻 16947.580 s。80 箱仍唯一交付，医疗和首批物资无硬时限违约，最小硬时限余量为 1437.908 s。运输加权延期为 1383223.556 优先系数·秒，高于问题二的纯运输值，反映在当前搜索方案中为通信协调付出的及时性代价。

#figure(
  placement: top,
  image("../../runs/reproduced/figures/q3_direct_coverage_map.pdf", width: 82%),
  caption: [固定网关直连可用性与地形遮挡分布。],
)

直连图显示沿线存在需要中继的盲区。因此只验证服务区端点或仅在配送完成时验证通信，不能满足题目对完整飞行和交接过程的要求。

#figure(
  placement: top,
  image("../../runs/reproduced/figures/q3_relay_layout.pdf", width: 82%),
  caption: [问题三运输航迹与四个中继悬停点。],
)

四个悬停点在不同时窗被重复利用，形成 10 个中继架次。中继空间位置与服务时段同时影响覆盖质量、返航能量和能源组件周转，不能只按几何距离选择最近点。

== 7.3 覆盖与资源核查

34919 个 1 秒采样点中，19865 点由 G01 直连，15054 点由可用中继链路覆盖，检出的中断点为 0。最大同一轨迹阶段采样间隔为 1.0 s；通信保障表记录每架运输机的“直连/中继”区间和对应中继架次。10 个中继架次的返航 SOC、两架实体中继机与 6 组能源组件的占用/充电区间，以及问题二全部运输约束均通过检查。

#figure(
  placement: top,
  image("../../runs/reproduced/figures/q3_communication_timeline.pdf", width: 82%),
  caption: [运输架次的直连和中继保障时序。],
)

时间线中每架次的通信状态由连续的区间构成。所谓“零中断”严格指当前 DEM、链路参数与 1 秒数值网格上未出现 OUTAGE；风雨衰落、设备容量和采样间隔内的瞬态变化不在这一结论之内。
