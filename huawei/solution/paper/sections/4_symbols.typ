= 4 主要符号

#figure(
  table(
    columns: (1.15fr, 3.8fr),
    stroke: 0.35pt,
    inset: 5pt,
    [符号], [含义与单位],
    [$i,j$], [调度中心或服务区节点],
    [$g$], [运输机机型 A、B、C],
    [$q, Q_g$], [当前有效载荷及机型额定最大载荷，kg],
    [$d_(i,j)$], [航段水平距离，m],
    [$h_(i,j)^+$], [航段爬升高度，m],
    [$L_g(q)$], [载荷 $q$ 下等效航程，m],
    [$E_p$], [架次能耗，kWh],
    [$rho_g$], [返航安全余量比例],
    [$T_b, tau_b$], [货箱实际交付时刻与要求时刻，s],
    [$W_k$], [第 $k$ 任务组的资源占用工作量，s],
  ),
  caption: [统一建模中使用的主要符号。],
)

所有时间均以任务开始后的秒为单位；返航 SOC 在正文以百分数表示。问题三的“通信覆盖点数”是时空轨迹采样记录数，并非独立服务区或独立架次数。
