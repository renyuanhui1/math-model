= 2 数据处理与统一物理模型

== 2.1 数据与航段

五个基础工作簿分别给出节点、逐箱需求与时限、运输机和电池、中继机和能源组件、无线链路参数。逐箱表中的“医疗物资”以期望送达时刻为硬截止；首批货箱以首批截止时刻为硬约束；其余箱的期望时刻用于计算软延期。模型读取 30 m DEM，逐节点检查高程与服务区编号，生成每个有向航段的水平距离、沿线最高地面高程、爬升和下降高度。对航段 $i\to j$，巡航海拔为沿线最高 DEM 值加 50 m；中心的起降高度为地面，服务区的交接高度为地面上方 30 m[1]。

== 2.2 时间、载荷与电量

令 $q$ 为航段当前有效载荷，$Q_g$ 为机型 $g$ 的额定载重，$L_g^0$ 和 $L_g^F$ 为其空载与满载等效航程。题给的载荷—航程关系写为

$ L_g(q) = L_g^0 - (L_g^0 - L_g^F) (q / Q_g)^(3/2). $

模型沿航段分别计算爬升、巡航、下降时间，并把水平等效航程能耗和爬升附加能耗相加。下降附加能耗按题设取零。对每一运输架次 $p$，逐站卸载后更新下一航段的 $q$；返航空载，且总能耗满足

$ E_p <= (1 - rho_g) E_g^"use". $

其中 $rho_g$ 为返航安全余量，$E_g^"use"$ 为单组电池可用能量。电池任务结束后依其剩余 SOC 按“0%—90% 快速阶段占完全充电时间的 65%、90%—100% 慢速阶段占 35%”换算至满电时间；充电完毕方能再次分配。所有架次的交付时刻含准备、装箱、飞行和交接，系统完成时间按最后一架相关无人机返航定义。

#figure(
  placement: top,
  image("../../runs/reproduced/figures/q1_dem_service_map.pdf", width: 82%),
  caption: [DEM 地形、调度中心与服务区位置。],
)

地形图显示服务区分布和高程差异。即使两点水平距离接近，沿线山脊仍会改变巡航高度、爬升能耗和后续通信视线，因此四问共享同一 DEM 航段库是结果可比的前提。
