= 8 问题四：固定任务的分区与独立资源配置

== 8.1 不可拆任务块与评价指标

问题四保持问题三货箱组批、访问顺序、运输与中继任务时刻及通信关系不变。将 15 个服务区作为超图顶点，每个访问多个服务区的运输架次连接其全部目的地；互相连通的超边压缩为不可拆任务块。当前联合方案产生 6 块。把这些块分到 K 个非空组，可以保证同一运输架次不跨组。对每一组，实体机需求取执行区间的最大并发数；电池与能源组件还把充电区间计入占用。原先由同一个中继架次同时保障不同组任务时，分区后各组要分别配置相应资源。

采用缺口优先的词典序评价：先最小化相对现有库存的资源缺口总数，再最小化相对集中调度的冗余，随后比较各组工作量变异系数和最大最小工作量差。工作量为运输与中继架次占用时间之和；服务区数量本身不能代表负荷。消除标签对称后，K=2 和 K=3 分别需枚举 31 与 90 种有效分区，因此结果是在固定任务结构下的精确分区选择。

== 8.2 配置方案与库存缺口

#figure(
  table(
    columns: (2fr, 1fr, 1fr, 1fr),
    stroke: 0.35pt,
    inset: 5pt,
    [指标], [集中调度], [K=2], [K=3],
    [A/B/C 运输机], [2/2/2], [2/3/2], [3/4/2],
    [A/B/C 电池], [2/4/4], [2/4/4], [3/5/4],
    [中继机/能源组件], [2/4], [3/5], [4/6],
    [总资源缺口], [0], [2], [5],
    [相对集中调度冗余], [0], [3], [9],
    [工作量变异系数], [—], [0.727909], [0.883321],
  ),
  caption: [固定问题三任务下的集中与独立分区资源需求。],
)

K=2 时，G2-2 仅包含 S012，G2-1 包含其余 14 个服务区；分区需 B 型运输机 3 架和中继机 3 架，而库存各为 2 架，故分别缺 1 架，总缺口 2。K=3 时，S012、S014 各自成组，其余 13 个服务区组成主体组；需 B 型运输机 4 架、B 型电池 5 组、中继机 4 架，较库存分别缺 2 架、1 组、2 架，总缺口 5。两个独立分区在当前库存下均不能原样执行，集中调度则无资源缺口。

#figure(
  placement: top,
  image("../../runs/reproduced/figures/q4_resource_comparison.pdf", width: 82%),
  caption: [集中调度与两种独立分区的资源需求比较。],
)

图中额外需求来自组间不能共享实体与中继保障。K=2 相比 K=3 缺口更少、冗余更低，但工作量变异系数仍达 0.727909，说明缺口优先目标并未同时实现充分均衡；若救援指挥更看重均衡，需要明确增加可用库存或重设任务结构，本问禁止为此改动问题三路线。
